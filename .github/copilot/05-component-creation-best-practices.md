# Component Creation Best Practices

1. Use real library rendering for capture; do not hand-draw approximations.
2. Use `Category / Variant` naming for clean Figma Assets grouping.
3. Preserve original dimensions when converting to components.
4. Validate no layout drift after conversion.
5. Keep capture helpers temporary; persist final specs/mappings in `figma/`.