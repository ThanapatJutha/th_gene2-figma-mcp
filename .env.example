# Create a Figma personal access token in Figma → Settings → Personal access tokens
FIGMA_TOKEN=

# Figma file key (the long id in the URL)
FIGMA_FILE_KEY=

# Optional: default output directory
OUTPUT_DIR=out
